import { MyShadowDirective } from './my-shadow.directive';

describe('MyShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new MyShadowDirective(null);
    expect(directive).toBeTruthy();
  });
});
